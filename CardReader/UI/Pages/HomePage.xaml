<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<Page
    x:Class="CardReader.UI.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CardReader.UI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="20" Margin="20 25" HorizontalAlignment="Center">
            <Button Content="{x:Bind ViewModel.IdReaderButtonText}" Command="{x:Bind ViewModel.NavigateToIdReaderCommand}" FontSize="36" Padding="50"/>
            <Button Content="{x:Bind ViewModel.DriverLicenseReaderButtonText}" Command="{x:Bind ViewModel.NavigateToDriverLicenseReaderCommand}" FontSize="36" Padding="50"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="20" Margin="20 25" HorizontalAlignment="Center">
            <Button Content="{x:Bind ViewModel.SettingsButtonText}" Command="{x:Bind ViewModel.NavigateToSettingsCommand}" FontSize="36" Padding="50"/>
        </StackPanel>
    </Grid>
</Page>
